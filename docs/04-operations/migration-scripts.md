# Migration Scripts

The `tools/migration-scripts` folder contains repo-maintenance utilities rather than runtime code.

## Current scripts

- downgrade target frameworks
- generate test projects
- generate sample projects
- generate migration progress trackers
- move content during modular package migration
- validate `M*` naming conventions
