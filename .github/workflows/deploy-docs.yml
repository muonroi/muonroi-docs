name: Deploy Docs

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, vps]
    timeout-minutes: 15

    steps:
      - name: Deploy docs
        run: /opt/muonroi/deploy-docs.sh
